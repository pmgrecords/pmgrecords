h1 this is artwork
#artwork(ng-controller='ArtworkController as artwork' class="container")
    .row
        img(ng-repeat='image in artwork.images' src="images/art/{{ image }}" class="img-responsive center-block thumbnail col-md-3")