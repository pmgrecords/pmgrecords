#artwork(ng-controller='ArtworkController as artwork' class="container")
    h1 this is artwork
        .row
            .artwork-tile(ng-repeat='image in artwork.images', ng-style='{ "background-image": "url(\'{{ image.url }}\')" }')