.contact.row
    .col-sm-12.contact-form-container
        form.form-horizontal(novalidate='novalidate', name='form', ng-show='!contact.emailSent')
            .form-group
                .col-sm-offset-2.col-sm-10
                    h3 SEND US A MESSAGE
            .form-group(ng-class="{ 'has-error' : form.email.$invalid && form.email.$touched }")
                label.control-label.col-sm-2 Email
                .col-sm-8
                    input.form-control(type='text', ng-model='contact.contactForm.email', name='email', maxlength='255', required='required', ng-pattern='/^[_a-z0-9]+(.[_a-z0-9]+)*@[a-z0-9-]+(.[a-z0-9-]+)*(.[a-z]{2,4})$/')
                    .error(ng-show='form.email.$touched')
                        span(ng-show='form.email.$error.required || form.email.$error.pattern') Email address is required.
            .form-group(ng-class="{ 'has-error' : form.message.$invalid && form.message.$touched }")
                label.control-label.col-sm-2 Message
                .col-sm-8
                    textarea.form-control(ng-model='contact.contactForm.message', name='message', required='required', rows='8')
                    .error(ng-show='form.message.$touched')
                        span(ng-show='form.message.$error.required') Message is required.
            .form-group
                .col-sm-offset-2.col-sm-10
                    button.btn.btn-warning(type='button', ng-disabled='form.$invalid', ng-click='contact.emailSent = true') Send
        h3.email-sent(ng-show='contact.emailSent')
            | Thanks for your feedback!
            br
            | We'll respond as soon as we can.